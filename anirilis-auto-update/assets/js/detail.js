
const data=window.ANIRILIS_DATA||[];const params=new URLSearchParams(location.search);const a=data.find(x=>x.id===params.get('id'))||data[0];const root=document.getElementById('detailRoot');const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));document.title=`${a.title} — AniRilis`;root.innerHTML=`<div class="detail-hero"><img class="detail-bg" src="${a.poster}" alt=""><div class="detail-poster"><img src="${a.poster}" alt="Poster ${esc(a.title)}"></div><div class="detail-copy"><div class="detail-meta"><span class="meta-pill">${a.type}</span><span class="meta-pill">${a.status}</span><span class="meta-pill">⭐ ${a.rating}</span><span class="meta-pill">${a.year}</span></div><h1>${esc(a.title)}</h1><div class="detail-meta">${a.genre.map(g=>`<span class="meta-pill">${esc(g)}</span>`).join('')}</div><p>${esc(a.synopsis)}</p><div class="small">Studio: ${esc(a.studio)} · Jadwal: ${a.day}, ${a.time}</div><div class="episode-box"><div class="episode-action"><div><strong>Episode ${a.episode} — ${esc(a.subtitle)}</strong><div class="small">Diperbarui ${esc(a.updated)}</div></div><button class="btn btn-primary" id="sourceBtn">Buka Halaman Sumber</button></div></div></div></div>`;document.getElementById('sourceBtn').onclick=()=>{if(a.sourceUrl){window.open(a.sourceUrl,'_blank','noopener,noreferrer')}else{alert('Tautan sumber belum tersedia.')}};document.getElementById('menuBtn').onclick=()=>document.getElementById('mobileNav').classList.toggle('is-open');
